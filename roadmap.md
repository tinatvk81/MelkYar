# نقشه راه پروژه: سامانه مدیریت فایل‌های املاک

## ۱. جمع‌بندی و اضافات پیشنهادی روی ایده شما

ایده‌ی شما کامل و درست است؛ چند نکته را اضافه/اصلاح می‌کنم:

- **ورود با تایید مدیر:** پیشنهاد من نسخه‌ی دوم شماست، نه اولی: **مدیر خودش برای هر مشاور اکانت (نام کاربری + رمز موقت) می‌سازد** و به او می‌دهد. دلیل: در حالت "ثبت‌نام آزاد + تایید مدیر"، هرکسی می‌تواند فرم ثبت‌نام را ببیند و پر کند که هم امنیتی نیست هم بار پشتیبانی اضافه می‌کند (پیام‌های تایید مکرر، ایمیل جعلی و ...). با ساخت مستقیم اکانت توسط مدیر: کنترل کامل دارید، در لحظه‌ی خروج مشاور فقط اکانتش را غیرفعال می‌کنید، و کاربر از روز اول رمز عبور دائمی (نه موقت) تنظیم می‌کند.
- **سطوح دسترسی:** نه فقط ۲ نقش، بلکه ۲ نقش با ۲ حالت مالکیت داده:
  - **مدیر (Admin):** دسترسی کامل به همه‌ی فایل‌ها (چه فایل‌های شخصی خودش چه فایل‌های همه‌ی مشاوران) + مدیریت کاربران + گزارش‌گیری کلی.
  - **مشاور (Agent):** فقط CRUD (دیدن/افزودن/ویرایش/حذف) روی فایل‌هایی که خودش ثبت کرده. فایل‌های مشاوران دیگر را اصلاً نمی‌بیند (نه فقط ویرایش، بلکه در لیست هم نیاید).
  - پیشنهاد اضافه: یک فلگ `is_private` روی فایل‌های خود مدیر که حتی اگر بعداً نقش سومی مثل «مدیر ارشد کمکی» اضافه شد، این فایل‌ها دست‌نخورده بمانند.
- **انواع معامله:** فروش، پیش‌خرید، اجاره، رهنِ کامل — دقیقاً درست است، چون فیلدهای مالی‌شان واقعاً فرق دارد. من در اکسل و دیتابیس این ۴ را با ستون‌های مشترک (متراژ، آدرس، امکانات) + ستون‌های اختصاصی طراحی کردم.
- **صفحه‌ی جستجوی یکپارچه (مثل دیوار):** درست است، من اسمش را در طراحی گذاشتم «فهرست فایل‌ها» با فیلترهای: نوع معامله، شهر/منطقه، بازه‌ی قیمت (از–تا)، متراژ (از–تا)، تعداد اتاق، آسانسور، پارکینگ، نوع سند، وضعیت فایل.
- **ماژول یادآوری تمدید اجاره/رهن:** ایده‌ی خیلی خوبیه و کاملاً قابل پیاده‌سازی است — جزئیات کامل در بخش ۴.
- **موارد اضافه‌ای که خودم پیشنهاد می‌کنم:**
  - **لاگ تغییرات (Activity Log):** هر بار فایلی ثبت/ویرایش/حذف شد، ثبت شود چه کسی و کی این کار را کرد (برای شفافیت بین مدیر و مشاوران).
  - **وضعیت فایل:** فعال / رزرو شده / معامله‌شده / غیرفعال — تا فایل‌های بسته‌شده در جستجوی مشتری نیایند ولی در آرشیو بمانند.
  - **گالری تصاویر برای هر فایل** (چند عکس، نه فقط یک لینک).
  - **شماره تماس مالک به‌صورت جدا از توضیحات عمومی** (تا در نسخه‌ی نمایش عمومی به مشتری، این شماره پنهان بماند و فقط مشاور/مدیر ببینند).
  - **گزارش عملکرد مشاوران:** تعداد فایل ثبت‌شده، تعداد معامله‌ی موفق در ماه (بعداً، فاز ۲).
  - **اعلان (Notification) داخل اپ + ایمیل/پیامک اختیاری** برای هشدار پایان قرارداد.

---

## ۲. ستون‌های اکسل (کامل)

فایل اکسل آماده را برایتان ساختم و پیوست کردم (۴ شیت: فروش، پیش‌خرید، اجاره، رهن_کامل + یک شیت راهنما). خلاصه‌ی ستون‌ها:

**ستون‌های مشترک همه‌ی انواع فایل:**
کد ملک، عنوان آگهی، نوع ملک (مسکونی/تجاری/اداری/زمین/ویلا)، استان، شهر، منطقه/محله، آدرس کامل، لینک موقعیت روی نقشه، متراژ زیربنا، متراژ زمین، تعداد اتاق خواب، سال ساخت، تعداد کل طبقات، طبقه‌ی واحد، تعداد واحد در طبقه، جهت ساختمان، نوع سند، آسانسور، پارکینگ، انباری، بالکن، امکانات رفاهی، سیستم گرمایش/سرمایش، کفپوش، وضعیت بازسازی، وضعیت واحد، نام و شماره مالک، نوع فایل (انحصاری/غیرانحصاری)، نام مشاور ثبت‌کننده، تاریخ ثبت و آخرین بروزرسانی، وضعیت فایل، توضیحات عمومی، یادداشت خصوصی مشاور، لینک تصاویر.

**فروش:** قیمت کل، قیمت هر متر، شرایط پرداخت، مبلغ پیش‌پرداخت، قابلیت معاوضه.

**پیش‌خرید:** قیمت کل قرارداد، شرکت/سازنده، درصد پیشرفت پروژه، تاریخ تحویل، مبلغ پرداخت‌شده، مبلغ باقی‌مانده، شرایط اقساط، شماره قرارداد.

**اجاره:** مبلغ ودیعه، اجاره‌ی ماهانه، تاریخ شروع/پایان قرارداد، «روز تا پایان قرارداد» (خودکار)، نام و تماس مستاجر فعلی، قابلیت تبدیل رهن↔اجاره، درصد افزایش سالانه، وضعیت تمدید، نتیجه‌ی آخرین تماس، تاریخ تماس بعدی.

**رهن‌کامل:** مبلغ رهن، تاریخ شروع/پایان قرارداد، «روز تا پایان قرارداد» (خودکار)، نام و تماس مستاجر فعلی، وضعیت تمدید، نتیجه‌ی آخرین تماس، تاریخ تماس بعدی.

> نکته: ستون‌های «نام/تماس مستاجر فعلی» دقیقاً همان چیزی است که برای سناریوی «۱۱ ماه پیش اجاره دادم، ۱ ماه مانده» لازم دارید — همین ستون‌ها هستند که موتور یادآوری از رویشان کار می‌کند.

---

## ۳. معماری کاربران و دسترسی

```
ورود با شماره‌موبایل/یوزرنیم + رمز عبور
        │
        ▼
   نقش کاربر؟
   ├── مدیر ──────► دسترسی کامل به: همه‌ی فایل‌های خودش + همه‌ی مشاوران
   │                + پنل مدیریت کاربران (ساخت/غیرفعال‌سازی/ریست رمز)
   │                + Import اکسل + گزارش‌ها
   └── مشاور ─────► فقط فایل‌هایی که خودش ثبت کرده (افزودن/ویرایش/حذف/جستجو)
                     + پروفایل شخصی
```

قوانین کلیدی:
1. هر رکورد فایل ملکی یک فیلد `owner_agent_id` دارد. کوئری‌های مشاور همیشه با `WHERE owner_agent_id = current_user.id` فیلتر می‌شوند (در سطح API، نه فقط UI — این خیلی مهم است تا مشاور با دستکاری URL هم نتواند فایل دیگری را ببیند).
2. غیرفعال‌سازی اکانت مشاور = `is_active=False`؛ فایل‌های او در دیتابیس می‌مانند (برای مدیر قابل مشاهده) ولی خودش دیگر نمی‌تواند وارد شود.
3. مدیر می‌تواند رمز مشاور را ریست کند (بدون نیاز به دیدن رمز قدیمی).

---

## ۴. ماژول یادآوری تمدید قرارداد (اجاره/رهن)

صفحه‌ی جداگانه‌ای به نام **«قراردادهای رو‌به‌اتمام»** با این منطق:

- هر روز (Cron Job / Scheduled Task) سیستم چک می‌کند: `تاریخ پایان قرارداد - امروز <= 30 روز`.
- این فایل‌ها در صفحه‌ی جدا با جزئیات زیر لیست می‌شوند: آدرس، مستاجر فعلی و شماره تماسش، مبلغ فعلی، چند روز مانده، وضعیت تماس (تماس نگرفته/گرفته/تصمیم مستاجر).
- مشاور/مدیر روی فایل کلیک می‌کند → دکمه «ثبت نتیجه تماس» با گزینه‌های: «تمایل به تمدید»، «تمایل به تخلیه»، «نامشخص/تماس مجدد».
- اگر «تمایل به تمدید» زده شود:
  - سیستم یک **فلگ «فورس/اولویت یک‌ماهه»** روی فایل می‌گذارد و این فایل در نتایج جستجوی عمومی (برای مشتری بعدی) با نشان ویژه («به‌زودی خالی می‌شود») نمایش داده می‌شود، حتی قبل از تمدید رسمی.
  - فیلد «شماره تماس ساکن فعلی» به همراه فایل نگه داشته می‌شود تا هماهنگی بازدید با مستاجر فعلی راحت باشد.
- بعد از تمدید یا تخلیه‌ی نهایی، مدیر/مشاور تاریخ قرارداد جدید را ثبت می‌کند و رکورد از لیست «رو‌به‌اتمام» خارج می‌شود.

این دقیقاً سناریوی خودتان (اجاره‌ی ۱۱ ماه پیش، ۱ ماه مانده) را پوشش می‌دهد.

---

## ۵. ماژول‌ها/صفحات پیشنهادی اپ

| ماژول | توضیح |
|---|---|
| ورود/احراز هویت | یوزرنیم+رمز، نقش‌محور |
| داشبورد | آمار سریع: تعداد فایل فعال، قراردادهای رو‌به‌اتمام، فایل‌های این هفته |
| فهرست فایل‌ها (جستجوی یکپارچه) | فیلتر نوع معامله/شهر/منطقه/بازه قیمت/متراژ/اتاق/آسانسور/پارکینگ/نوع سند |
| فروش | لیست + افزودن/ویرایش + Import اکسل |
| پیش‌خرید | همان‌طور |
| اجاره | همان‌طور |
| رهن‌کامل | همان‌طور |
| قراردادهای رو‌به‌اتمام | طبق بخش ۴ |
| Import اکسل | آپلود فایل، پیش‌نمایش، نگاشت ستون‌ها، اعتبارسنجی خطاها قبل از ثبت نهایی |
| مدیریت کاربران (فقط مدیر) | ساخت/ویرایش/غیرفعال‌سازی مشاور، ریست رمز |
| پروفایل من | برای هر کاربر |
| گزارش‌ها (فاز ۲) | عملکرد مشاوران، تعداد معاملات |

---

## ۶. پیشنهاد فنی (Tech Stack)

با توجه به تجربه‌ی شما در **Django، Node.js، Python، Java**:

**پیشنهاد اصلی: Django + Django REST Framework (بک‌اند) + React (فرانت‌اند وب)**

چرا Django بهتر از Node.js برای این پروژه است:
- مدل داده‌ی شما (فایل ملکی با چند زیرنوع، کاربران با نقش، لاگ فعالیت) دقیقاً همان چیزی است که **Django ORM + Django Admin** برایش ساخته شده — پنل ادمین آماده‌ی Django می‌تواند در همان هفته‌ی اول برای شما (بدون کدنویسی UI) قابل استفاده باشد تا زمانی که پنل اختصاصی آماده شود.
- احراز هویت، نقش‌بندی (Permission/Group)، و کنترل دسترسی ردیفی (row-level: مشاور فقط فایل خودش) در Django با `django-guardian` یا فیلتر ساده‌ی QuerySet به‌سادگی پیاده می‌شود.
- Import اکسل: کتابخانه‌ی `openpyxl`/`pandas` مستقیم در پایتون، بدون نیاز به سرویس جدا.
- شما با پایتون/جنگو راحت هستید، پس سرعت توسعه بالاتر می‌رود.

**پشته‌ی پیشنهادی نهایی:**
- بک‌اند: Django + Django REST Framework + PostgreSQL
- فرانت‌اند وب: React (یا حتی سریع‌تر برای MVP: Django Templates + Bootstrap، بعداً React اضافه شود)
- احراز هویت: JWT یا session-based Django auth
- زمان‌بند یادآوری: Celery + Redis (یا برای شروع ساده: یک Cron job روزانه)
- هاست اولیه: یک VPS ساده (لیارا، آرون، یا هر ابر ایرانی) + دامنه
- بعداً (فاز ۳، اختیاری): اپ موبایل با React Native برای مشاوران در حال بازدید ملک

اگر فقط MVP سریع و ارزان می‌خواهید و تیم فنی کوچک دارید، **Django Templates + Bootstrap** برای شروع کافی است و بعداً می‌شود API جدا کرد و React اضافه کرد — نیازی نیست از روز اول React کامل بسازید.

---

## ۷. نقشه راه اجرا (فازبندی)

**فاز ۰ — آماده‌سازی داده (الان):**
1. فایل اکسل پیوست را باز کنید، شیت راهنما را بخوانید.
2. فایل‌های واقعی خودتان را داخل ۴ شیت (فروش/پیش‌خرید/اجاره/رهن_کامل) بریزید — حتی اگر اول در همین اکسل کار کنید، بعداً مستقیم Import می‌شود.
3. لیست اسامی مشاوران فعلی و شماره تماسشان را آماده کنید (برای ساخت اکانت).

**فاز ۱ — هسته‌ی سیستم (MVP):** ✅ کامل (بک‌اند)
- ساخت پروژه‌ی Django + مدل‌های User (نقش مدیر/مشاور)، Property (پایه) و ۴ زیرمدل معامله.
- صفحه‌ی ورود + کنترل دسترسی نقش‌محور.
- پنل مدیریت کاربران برای مدیر (ساخت/غیرفعال‌سازی).
- فرم افزودن/ویرایش دستی فایل برای هر ۴ نوع (به‌صورت API).
- صفحه‌ی فهرست + فیلترهای اصلی (شهر/منطقه/بازه قیمت/متراژ).

**فاز ۲ — Import و اتوماسیون:** ✅ کامل
- ماژول Import اکسل (آپلود، نگاشت ستون، گزارش خطا per-row).
- ماژول «قراردادهای رو‌به‌اتمام» + فلگ اولویت یک‌ماهه.
- لاگ فعالیت کاربران.

**فاز ۲.۵ — امنیت Production:** ✅ کامل
- مدیریت secrets با `.env`، ضدBruteforce (`django-axes`)، Rate limiting، هدرهای HTTPS، بلاک‌کردن توکن هنگام غیرفعال‌سازی مشاور، اعتبارسنجی آپلود تصویر، لاگ‌گیری.

**فاز ۲.۷ — استقرار (Docker):** ✅ کامل
- `Dockerfile` (اجرای غیر-روت، gunicorn)، `docker-compose.yml` (web + postgres + nginx)، `entrypoint.sh` (انتظار برای دیتابیس + migrate خودکار + collectstatic)، تنظیمات nginx به‌عنوان reverse proxy.
- با یک دستور (`docker compose up --build`) کل سیستم (اپ + دیتابیس + وب‌سرور) بالا می‌آید — محیط dev و production یکسان می‌مانند.

**فاز ۳ — تکمیلی:** 🔲 باقی‌مانده
- فرانت‌اند (React یا Django Templates) — بزرگ‌ترین بخش باقی‌مانده.
- گزارش عملکرد مشاوران.
- گالری چند‌عکسی برای هر فایل (مدل آماده است، UI آپلود باقی مانده).
- اعلان پیامکی/ایمیلی برای هشدار قرارداد (منطق در `check_renewals` آماده، فقط اتصال به سرویس پیامک باقی مانده).
- تست‌های خودکار (pytest/unittest) — قبل از سپردن به production واقعی توصیه می‌شود.
- نسخه‌ی موبایل (اختیاری).

---

## ۸. قدم بعدی پیشنهادی

هر کدام آماده باشید بگویید تا شروع کنیم:
1. من همین الان **مدل داده‌ی Django (models.py)** را دقیقاً بر اساس همین ستون‌های اکسل برایتان بنویسم، یا
2. اول برویم روی **طراحی صفحات (Wireframe)** فهرست فایل‌ها و فرم افزودن ملک، یا
3. اول فایل‌های واقعی‌تان را در همین اکسل کامل کنید و بعد شروع کنیم.
