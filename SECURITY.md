# چک‌لیست امنیتی — قبل از استفاده‌ی واقعی (Production)

کدهایی که تا اینجا اضافه شدند (در همین commit):
- ✅ `SECRET_KEY`، رمز دیتابیس و همه‌ی مقادیر حساس از `.env` خوانده می‌شوند، نه هاردکد در کد
- ✅ `DEBUG=False` پیش‌فرض؛ اگر خاموش نباشد و `ALLOWED_HOSTS` درست نباشد، سرور اصلاً بالا نمی‌آید
- ✅ هدرهای امنیتی HTTP (HSTS، X-Frame-Options، nosniff) در production فعال
- ✅ CORS محدود به دامنه‌ی فرانت‌اند واقعی شما (نه باز برای همه)
- ✅ ضد Brute-force با `django-axes`: بعد از ۵ بار رمز اشتباه، اکانت ۳۰ دقیقه قفل می‌شود
- ✅ Rate limiting روی API (ناشناس ۲۰ درخواست/دقیقه، کاربر وارد‌شده ۱۲۰/دقیقه)
- ✅ عمر کوتاه‌تر access token (۲ ساعت) + امکان باطل‌کردن فوری توکن‌ها هنگام غیرفعال‌سازی مشاور
- ✅ اعتبارسنجی نوع و حجم فایل برای آپلود تصاویر (جلوی آپلود فایل اجرایی با پسوند جعلی)
- ✅ لاگ‌گیری فایل + کنسول برای خطاها و رخدادهای امنیتی
- ✅ کنترل دسترسی ردیفی مشاور/مدیر که از قبل در مدل و ViewSet پیاده شده

## کارهایی که باید قبل از رفتن روی سرور واقعی حتماً انجام دهید

1. **HTTPS واقعی**: از یک reverse proxy مثل Nginx با گواهی SSL (رایگان با Let's Encrypt) استفاده کنید. بدون HTTPS، تنظیمات `SECURE_SSL_REDIRECT`/`HSTS` معنی ندارند و رمز کاربران باز روی شبکه رد می‌شود.
2. **دیتابیس PostgreSQL روی سرور جدا یا حداقل با فایروال محدود** — پورت ۵۴۳۲ نباید از اینترنت باز باشد.
3. **بک‌آپ خودکار دیتابیس** (روزانه، با نگهداری چند نسخه‌ی قدیمی) — یک خط اطلاعات مشتری/مالک/مستاجر ارزش بازیابی دارد.
4. **آدرس پنل ادمین را عوض کنید**: به‌جای `/admin/` یک مسیر تصادفی بگذارید (`/staff-x7k2-panel/`) تا در برابر اسکن‌های خودکار کمی سخت‌تر شود؛ این جایگزین احراز هویت نیست، فقط یک لایه‌ی اضافه است.
5. **دو مرحله‌ای برای اکانت مدیر (2FA)**: حداقل برای اکانت مدیر، افزودن `django-otp` یا مشابه، چون این اکانت به همه‌چیز دسترسی دارد.
6. **بروزرسانی منظم پکیج‌ها**: هر چند وقت یک‌بار `pip list --outdated` بزنید، خصوصاً Django/DRF/simplejwt که مستقیماً امنیت را لمس می‌کنند.
7. **مانیتورینگ خطا** (اختیاری ولی خیلی مفید): سرویس رایگان/ارزان مثل Sentry برای دیدن خطاهای production بدون نیاز به چک دستی لاگ.
8. **دسترسی SSH فقط با کلید**، نه رمز عبور؛ کاربر root غیرفعال باشد.
9. **جدا نگه‌داشتن محیط توسعه از production**: پایگاه‌داده‌ی تستی هرگز نباید همان پایگاه‌داده‌ی واقعی باشد.
10. **تست دسترسی**: قبل از رفتن روی سرور واقعی، با یک اکانت مشاور تست کنید که واقعاً نمی‌تواند فایل مشاور دیگر را از طریق تغییر مستقیم `id` در URL ببیند (این پروژه از قبل این را در سطح کوئری مسدود کرده، ولی تست دستی خیالتان را راحت می‌کند).

## نکته درباره‌ی داده‌های حساس (شماره تماس مالک/مستاجر)
این پروژه شماره تلفن اشخاص واقعی (نه فقط کاربران سیستم) را نگه می‌دارد.
پیشنهاد می‌کنم:
- این داده‌ها فقط برای مدیر و مشاورِ صاحب فایل قابل مشاهده باشد (که همین الان هست)
- در `public_description` که قرار است به مشتری نمایش داده شود، هرگز شماره‌ی مالک/مستاجر را نگذارید — این فیلد از فیلدهای `owner_phone`/`current_tenant_phone` جداست، دقیقاً برای همین منظور.
